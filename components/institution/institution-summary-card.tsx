"use client"

import type { Institution } from "@/lib/types"

/**
 * 制度の概要を表示するカードコンポーネント
 *
 * @param institution - 対象の制度
 */
export function InstitutionSummaryCard({ institution }: { institution: Institution }) {
  // 実装は既存のコードを移行する
  return (
    <div>
      <h2>Institution Summary (Placeholder)</h2>
      <p>This component will be implemented later</p>
    </div>
  )
}

